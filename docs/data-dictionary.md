
# **`data-dictionary.md`**
```markdown
# 📚 Data Dictionary  
Definitions and descriptions of all fields used across raw, normalized, and forecast datasets.

---

## 1. Purpose  
This document provides a standardized reference for all data elements processed by the capacity pipeline. It ensures consistency across ingestion, normalization, analytics, and reporting.

---

## 2. Dataset Categories  
- **Raw Metrics** — Directly collected from API sources  
- **Normalized Metrics** — Schema‑aligned, cleaned, and enriched  
- **Forecast Metrics** — Predictive outputs generated by analytics engine  

---

## 3. Field Definitions  

### 3.1 Common Fields  
| Field | Type | Description |
|-------|------|-------------|
| `timestamp` | datetime | UTC timestamp of metric sample |
| `source` | string | Origin system or API endpoint |
| `environment` | string | Environment identifier (sanitized) |

---

### 3.2 Raw Metrics  
| Field | Type | Description |
|-------|------|-------------|
| `cpu_usage` | float | CPU utilization percentage |
| `memory_usage` | float | Memory consumption percentage |
| `workflow_count` | integer | Number of workflows executed |
| `node_id` | string | Sanitized node identifier |

---

### 3.3 Normalized Metrics  
| Field | Type | Description |
|-------|------|-------------|
| `cpu_normalized` | float | CPU usage normalized to baseline |
| `memory_normalized` | float | Memory usage normalized to baseline |
| `workflow_rate` | float | Workflows per minute |
| `capacity_score` | float | Composite capacity indicator |

---

### 3.4 Forecast Metrics  
| Field | Type | Description |
|-------|------|-------------|
| `cpu_forecast` | float | Predicted CPU utilization |
| `memory_forecast` | float | Predicted memory utilization |
| `workflow_forecast` | float | Predicted workflow volume |
| `risk_level` | string | Low / Medium / High saturation risk |

---

## 4. Schema Notes  
- All timestamps use ISO‑8601 format  
- All numeric values use decimal precision  
- All identifiers are sanitized and non‑proprietary  
- Normalized fields follow baseline configuration rules  

---

## 5. Revision History  
| Version | Date | Description |
|--------|------|-------------|
| 1.0 | Initial | First sanitized data dictionary version |
